% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bar_charts.R
\name{bar_stacked}
\alias{bar_stacked}
\title{Stacked bar chart for a categorical x numeric variable, grouped by a third
factor}
\usage{
bar_stacked(
  dat,
  x,
  y,
  fill,
  x_lab = NULL,
  y_lab = NULL,
  fill_lab = NULL,
  title = NULL,
  base_size = 11,
  horiz = TRUE,
  label_digits = 1,
  label_size = NULL,
  label_style = "normal",
  fill_family = NULL,
  legend_position = "bottom",
  plot_width = 7,
  plot_height = 4
)
}
\arguments{
\item{dat}{A data frame, one row per x/fill combination.}

\item{x}{Unquoted column name for the x-axis category. Past 8 levels,
when \code{horiz = FALSE}, the category labels switch from wrapped/
horizontal to a single angled line (45 degrees) instead of combining
the two. Skipped entirely when \code{horiz = TRUE} (the default here) - a
flipped chart doesn't have the same crowding problem.}

\item{y}{Unquoted column name for the already-computed value each bar shows.}

\item{fill}{Unquoted column name for the grouping variable mapped to fill
colour.}

\item{x_lab}{Optional character string for the x-axis title.}

\item{y_lab}{Optional character string for the y-axis title.}

\item{fill_lab}{Optional character string for the legend title.}

\item{title}{Optional plot title - left empty if not supplied.}

\item{base_size}{Base font size in pt - scales all text via
\code{\link[=theme_house]{theme_house()}}. Default \code{11}.}

\item{horiz}{\code{TRUE} (default, matching this function's original
always-flipped behaviour) for horizontal bars, \code{FALSE} for upright -
done by swapping which column maps to the x vs y aesthetic, not
\code{coord_flip()} - see \code{\link[=bar]{bar()}}'s own documentation for the full explanation.}

\item{label_digits}{Decimal places shown on each segment's value label.
Default \code{1}.}

\item{label_size}{Font size in pt for the value labels. Default \code{NULL}
(falls back to \code{base_size}), independent of \code{base_size}.}

\item{label_style}{\code{"normal"} (default), \code{"suppress"} (no value labels),
or \code{"diag"} (labels rotated 45 degrees) - see \code{\link[=bar]{bar()}}'s own documentation.}

\item{fill_family}{\code{NULL} (default) colours \code{fill}'s levels categorically,
spread across nc_alt1 families (\code{\link[=scale_fill_pal_categorical]{scale_fill_pal_categorical()}}) - the
original, always-on behaviour. Set to a family name (e.g. \code{"mint"}) to
colour them sequentially within that one family instead
(\code{\link[=scale_fill_pal_sequential]{scale_fill_pal_sequential()}}) - suited to an ordered \code{fill} variable,
where staying in one hue lets the tint ramp itself carry the ordering.
No cap on levels either way - \code{scale_fill_pal_sequential()} interpolates
past 5.}

\item{legend_position}{\code{"bottom"} (default) or \code{"right"} - use \code{"right"}
for long fill labels.}

\item{plot_width}{Estimated plot width in inches - feeds label-wrapping
only, not the actual rendered size (see \code{\link[=bar]{bar()}}'s own documentation) -
keep in sync with the chunk's \code{fig-width} or \code{\link[=finalise_plot]{finalise_plot()}}'s
\code{ggsave()} width. Default \code{7}.}

\item{plot_height}{Estimated plot height in inches - feeds the same
wrapping maths for whichever label ends up on the left margin. Default
\code{4}. Same caveat as \code{plot_width}.}
}
\value{
A ggplot object. Fill colours always come from \link{nc_alt1}, never
\code{house_colours}.
}
\description{
Same idea as \code{\link[=bar_clustered]{bar_clustered()}}, but stacks segments into one bar per x
category (\code{position = "stack"}) instead of placing them side by side -
suited to more x categories, or where the total across \code{fill} levels
matters as much as each individual segment.
}
