% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_alt1.R
\name{scale_fill_pal_categorical}
\alias{scale_fill_pal_categorical}
\alias{scale_colour_pal_categorical}
\title{Categorical fill/colour scales from nc_alt1}
\usage{
scale_fill_pal_categorical(n = 12, primary = 1, families = names(nc_alt1))

scale_colour_pal_categorical(n = 12, primary = 1, families = names(nc_alt1))
}
\arguments{
\item{n}{Number of levels to colour. Default \code{12}.}

\item{primary}{Which of a family's 5 shades to use first, when \code{n} is
small enough that only one tier per family is needed. Default \code{1}.}

\item{families}{Character vector of \link{nc_alt1} family names to draw from.
Default all 6 (\code{grey} included).}
}
\value{
A ggplot2 scale object (\code{scale_fill_manual()}/
\code{scale_colour_manual()} under the hood).
}
\description{
For an unordered categorical variable - each level just needs to read as
visually distinct from the others, with no implied sequence between them.
Colours come from \code{\link[=give_categorical_colours_pal]{give_categorical_colours_pal()}}; see that function for
how many colours per family and in what order.
}
