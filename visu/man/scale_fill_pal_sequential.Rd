% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_alt1.R
\name{scale_fill_pal_sequential}
\alias{scale_fill_pal_sequential}
\alias{scale_colour_pal_sequential}
\title{Sequential (single-hue) fill/colour scales from nc_alt1}
\usage{
scale_fill_pal_sequential(family, n)

scale_colour_pal_sequential(family, n)
}
\arguments{
\item{family}{A single \link{nc_alt1} family name (e.g. \code{"blue"}, \code{"mint"}).}

\item{n}{Number of levels to colour. No cap - interpolates past 5.}
}
\value{
A ggplot2 scale object (\code{scale_fill_manual()}/
\code{scale_colour_manual()} under the hood).
}
\description{
For levels that all belong to \emph{one} colour family rather than being
spread across several - e.g. sub-categories of a single concept, or an
ordered/ranked categorical variable (a Likert scale, say), where staying
within one hue and varying only tint signals they're related and ordered,
rather than distinct topics.
}
\details{
Up to 5 levels, this pulls the family's 5 discrete shades (via
\code{\link[=give_categorical_colours_pal]{give_categorical_colours_pal()}}, restricted to one family); past 5, a
family only has 5 real shades to give, so this switches to interpolating
a continuous ramp between the family's palest and most saturated shade
instead (via \code{\link[=gradient_pal]{gradient_pal()}}), rather than spilling into another family
or erroring.
}
