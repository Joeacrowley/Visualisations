% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bar_charts.R
\name{bar}
\alias{bar}
\title{Bar chart for a single categorical x numeric variable}
\usage{
bar(
  dat,
  x,
  y,
  x_lab = NULL,
  y_lab = NULL,
  title = NULL,
  fill_family = "pink",
  base_size = 11,
  horiz = FALSE,
  label_digits = 1,
  label_size = NULL,
  label_style = "normal",
  plot_width = 7,
  plot_height = 4,
  fill = NULL
)
}
\arguments{
\item{dat}{A data frame, one row per bar.}

\item{x}{Unquoted column name for the x-axis category - ideally a factor.
Past 8 levels, when \code{horiz = FALSE}, the category labels switch from
wrapped/horizontal to a single angled line (45 degrees) instead of
combining the two. Skipped entirely when \code{horiz = TRUE} - a flipped
chart doesn't have the same crowding problem.}

\item{y}{Unquoted column name for the already-computed value each bar shows.}

\item{x_lab}{Optional character string for the x-axis title.}

\item{y_lab}{Optional character string for the y-axis title.}

\item{title}{Optional plot title - left empty if not supplied.}

\item{fill_family}{One of:
\itemize{
\item an \link{nc_alt1} colour name (e.g. \code{"pink"}, \code{"blue3"}, \code{"grey5"} - family
name alone defaults to shade 1, see \code{\link[=resolve_pal_colour]{resolve_pal_colour()}}) for a
single colour applied to every bar
\item \code{"multi"} to colour each bar differently, spread across all nc_alt1
families, via \code{\link[=give_categorical_colours_pal]{give_categorical_colours_pal()}}
\item \code{"<family>_seq"} (e.g. \code{"blue_seq"}) to colour each bar a different
shade within that one family's tint ramp instead - only meaningful
for an ordered x variable, and capped at 5 bars (one family only has
5 shades)
}

Default \code{"pink"}. Named to match \code{\link[=bar_clustered]{bar_clustered()}}/\code{\link[=bar_stacked]{bar_stacked()}}'s own
\code{fill_family} argument - \code{bar()} has no separate grouping column for
\code{fill} to mean "which column maps to colour" the way it does on those
two, so this argument has always meant "colour selection", just kept
under a consistent name.}

\item{base_size}{Base font size in pt - scales all text via
\code{\link[=theme_house]{theme_house()}}. Default \code{11}.}

\item{horiz}{\code{FALSE} (default) for upright bars, \code{TRUE} for horizontal.}

\item{label_digits}{Decimal places shown on each bar's value label.
Default \code{1}. Set to \code{0} for whole-number data (e.g. counts) where a
trailing ".0" just wastes space.}

\item{label_size}{Font size in pt for the value labels. Default \code{NULL}
(falls back to \code{base_size}). Independent of \code{base_size} so the labels
can be shrunk without shrinking the theme's title/axis/legend text too -
useful once many bars/dodge groups crowd the labels together.}

\item{label_style}{\code{"normal"} (default) - labels placed as usual.
\code{"suppress"} - no value labels at all. \code{"diag"} - value labels rotated
45 degrees, meant for the upright case (\code{horiz = FALSE}); still just a
text angle, so it won't error if used with \code{horiz = TRUE}, but isn't
really intended for it.}

\item{plot_width}{Estimated plot width in inches - feeds the
label-wrapping maths only, default \code{7}. Doesn't set the actual rendered
size - that's controlled separately, by the chunk's \code{fig-width}/
\code{fig-height} when knitting, or by \code{\link[=finalise_plot]{finalise_plot()}}'s \code{ggsave()}
dimensions when exporting. Keep this in sync with whichever of those
applies, or the wrapping will assume a different size than the plot
actually renders at.}

\item{plot_height}{Estimated plot height in inches - feeds the same
wrapping maths for whichever label ends up on the left margin. Same
caveat as \code{plot_width}.}

\item{fill}{Does nothing - present only so \code{bar()} accepts (and ignores)
the same \code{fill} argument \code{\link[=bar_clustered]{bar_clustered()}}/\code{\link[=bar_stacked]{bar_stacked()}} use for their
grouping column, for cross-function compatibility when a caller
dispatches between the three (e.g. \code{rlang::inject()}-built calls that
splice in \code{fill} unconditionally, whichever of the three ends up called).}
}
\value{
A ggplot object.
}
\description{
Plots statistics already calculated and sitting in a data frame - one row
per bar, an x category paired with an already-computed y value
(\code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}}'s job) - rather than counting raw observations
itself (\code{\link[ggplot2:geom_bar]{ggplot2::geom_bar()}}'s job). \code{dat} needs to already be aggregated
(e.g. via \code{count()}/\code{summarise()}, or \code{\link[dtab:calc_stats]{dtab::calc_stats()}}) before it's
passed in.
}
\details{
\code{horiz} is implemented by swapping which column maps to the x vs y
aesthetic (with \code{geom_col(orientation = )}'s help), not \code{coord_flip()} -
so \code{axis.title.x}/\code{axis.text.x} etc. always just mean whichever column
actually ends up mapped to x, nothing to translate. Flipping swaps which
axis sits on the narrow left margin vs the wide bottom, so whichever of
\code{x_lab}/\code{y_lab} is now on the left gets the "rotate vertical + wrap if
long" treatment instead of it always being \code{y_lab}.
}
